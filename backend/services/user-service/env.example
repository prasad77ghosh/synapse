# User Service environment example
# Copy to .env and edit values for your environment

# Service port
PORT=3002

# Primary database used by Prisma (PostgreSQL)
# Example: postgresql://app_user:app_password@localhost:5432/app_db?schema=public
DATABASE_URL=postgresql://app_user:app_password@localhost:5432/app_db?schema=public

# Redis (cache)
REDIS_HOST=localhost
REDIS_PORT=6379

# Kafka bootstrap (use localhost:29092 for services running on host)
KAFKA_BROKERS=localhost:29092

# Optional: Prisma-specific envs
# PRISMA_CLIENT_ENGINE_TYPE=binary
