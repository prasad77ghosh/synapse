
// command to generate proto type file
protoc \
  --plugin=./node_modules/.bin/protoc-gen-ts_proto \
  --ts_proto_out=./src/proto \
  --proto_path=../../proto \
  --ts_proto_opt=nestJs=true,esModuleInterop=true,outputServices=grpc-js,fileSuffix=.pb \
  ../../proto/*.proto

// command to start dev docker compose
docker compose -f docker-compose.dev.yml up -d

// to create new migration
npx prisma migrate dev --name <migration name>

// relode conatiner
docker exec -it kong kong reload



// kong database migration 
docker compose -f docker-compose.dev.yml run --rm kong kong migrations bootstrap
docker compose -f docker-compose.dev.yml run --rm -v $(pwd)/api-gateway/config:/kong/declarative kong kong config db_import /kong/declarative/kong.yml